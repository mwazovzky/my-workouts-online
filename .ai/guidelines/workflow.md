# Workflow Rules

## Documentation

- Always update relevant documentation (`docs/`) after changing business logic, routes, or architecture.
- Run `php artisan boost:install` after updating guidelines or documentation to keep Boost in sync.
